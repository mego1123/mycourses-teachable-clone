// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.27.0
// source: messages.sql

package gen

import (
	"context"
	"encoding/json"

	"github.com/google/uuid"
)

const countUnreadMessages = `-- name: CountUnreadMessages :one
SELECT COUNT(*) FROM messages WHERE user_id = $1 AND is_read = FALSE
`

func (q *Queries) CountUnreadMessages(ctx context.Context, userID uuid.UUID) (int64, error) {
	row := q.db.QueryRow(ctx, countUnreadMessages, userID)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const createMessage = `-- name: CreateMessage :one
INSERT INTO messages (user_id, tenant_id, type, title, body, metadata) VALUES ($1, $2, $3, $4, $5, $6) RETURNING id, user_id, tenant_id, type, title, body, is_read, metadata, created_at
`

type CreateMessageParams struct {
	UserID   uuid.UUID       `json:"user_id"`
	TenantID *uuid.UUID      `json:"tenant_id"`
	Type     string          `json:"type"`
	Title    string          `json:"title"`
	Body     string          `json:"body"`
	Metadata json.RawMessage `json:"metadata"`
}

func (q *Queries) CreateMessage(ctx context.Context, arg CreateMessageParams) (Message, error) {
	row := q.db.QueryRow(ctx, createMessage,
		arg.UserID,
		arg.TenantID,
		arg.Type,
		arg.Title,
		arg.Body,
		arg.Metadata,
	)
	var i Message
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.TenantID,
		&i.Type,
		&i.Title,
		&i.Body,
		&i.IsRead,
		&i.Metadata,
		&i.CreatedAt,
	)
	return i, err
}

const listMessagesByUser = `-- name: ListMessagesByUser :many
SELECT id, user_id, tenant_id, type, title, body, is_read, metadata, created_at FROM messages WHERE user_id = $1 ORDER BY created_at DESC LIMIT $2 OFFSET $3
`

type ListMessagesByUserParams struct {
	UserID uuid.UUID `json:"user_id"`
	Limit  int32     `json:"limit"`
	Offset int32     `json:"offset"`
}

func (q *Queries) ListMessagesByUser(ctx context.Context, arg ListMessagesByUserParams) ([]Message, error) {
	rows, err := q.db.Query(ctx, listMessagesByUser, arg.UserID, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Message{}
	for rows.Next() {
		var i Message
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.TenantID,
			&i.Type,
			&i.Title,
			&i.Body,
			&i.IsRead,
			&i.Metadata,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const markMessageRead = `-- name: MarkMessageRead :exec
UPDATE messages SET is_read = TRUE WHERE id = $1 AND user_id = $2
`

type MarkMessageReadParams struct {
	ID     uuid.UUID `json:"id"`
	UserID uuid.UUID `json:"user_id"`
}

func (q *Queries) MarkMessageRead(ctx context.Context, arg MarkMessageReadParams) error {
	_, err := q.db.Exec(ctx, markMessageRead, arg.ID, arg.UserID)
	return err
}
