// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.27.0
// source: certificates.sql

package gen

import (
	"context"

	"github.com/google/uuid"
)

const createCertificate = `-- name: CreateCertificate :one
INSERT INTO certificates (enrollment_id, user_id, course_id, tenant_id, certificate_number, verification_token, learner_name, course_title, creator_name)
VALUES ($1, $2, $3, $4, $5, $6, $7, $8, $9) RETURNING id, enrollment_id, user_id, course_id, tenant_id, certificate_number, verification_token, learner_name, course_title, creator_name, issued_at, status, revoked_reason, revoked_at, created_at
`

type CreateCertificateParams struct {
	EnrollmentID      uuid.UUID `json:"enrollment_id"`
	UserID            uuid.UUID `json:"user_id"`
	CourseID          uuid.UUID `json:"course_id"`
	TenantID          uuid.UUID `json:"tenant_id"`
	CertificateNumber string    `json:"certificate_number"`
	VerificationToken string    `json:"verification_token"`
	LearnerName       string    `json:"learner_name"`
	CourseTitle       string    `json:"course_title"`
	CreatorName       string    `json:"creator_name"`
}

func (q *Queries) CreateCertificate(ctx context.Context, arg CreateCertificateParams) (Certificate, error) {
	row := q.db.QueryRow(ctx, createCertificate,
		arg.EnrollmentID,
		arg.UserID,
		arg.CourseID,
		arg.TenantID,
		arg.CertificateNumber,
		arg.VerificationToken,
		arg.LearnerName,
		arg.CourseTitle,
		arg.CreatorName,
	)
	var i Certificate
	err := row.Scan(
		&i.ID,
		&i.EnrollmentID,
		&i.UserID,
		&i.CourseID,
		&i.TenantID,
		&i.CertificateNumber,
		&i.VerificationToken,
		&i.LearnerName,
		&i.CourseTitle,
		&i.CreatorName,
		&i.IssuedAt,
		&i.Status,
		&i.RevokedReason,
		&i.RevokedAt,
		&i.CreatedAt,
	)
	return i, err
}

const getCertificateByEnrollment = `-- name: GetCertificateByEnrollment :one
SELECT id, enrollment_id, user_id, course_id, tenant_id, certificate_number, verification_token, learner_name, course_title, creator_name, issued_at, status, revoked_reason, revoked_at, created_at FROM certificates WHERE enrollment_id = $1
`

func (q *Queries) GetCertificateByEnrollment(ctx context.Context, enrollmentID uuid.UUID) (Certificate, error) {
	row := q.db.QueryRow(ctx, getCertificateByEnrollment, enrollmentID)
	var i Certificate
	err := row.Scan(
		&i.ID,
		&i.EnrollmentID,
		&i.UserID,
		&i.CourseID,
		&i.TenantID,
		&i.CertificateNumber,
		&i.VerificationToken,
		&i.LearnerName,
		&i.CourseTitle,
		&i.CreatorName,
		&i.IssuedAt,
		&i.Status,
		&i.RevokedReason,
		&i.RevokedAt,
		&i.CreatedAt,
	)
	return i, err
}

const getCertificateByID = `-- name: GetCertificateByID :one
SELECT id, enrollment_id, user_id, course_id, tenant_id, certificate_number, verification_token, learner_name, course_title, creator_name, issued_at, status, revoked_reason, revoked_at, created_at FROM certificates WHERE id = $1
`

func (q *Queries) GetCertificateByID(ctx context.Context, id uuid.UUID) (Certificate, error) {
	row := q.db.QueryRow(ctx, getCertificateByID, id)
	var i Certificate
	err := row.Scan(
		&i.ID,
		&i.EnrollmentID,
		&i.UserID,
		&i.CourseID,
		&i.TenantID,
		&i.CertificateNumber,
		&i.VerificationToken,
		&i.LearnerName,
		&i.CourseTitle,
		&i.CreatorName,
		&i.IssuedAt,
		&i.Status,
		&i.RevokedReason,
		&i.RevokedAt,
		&i.CreatedAt,
	)
	return i, err
}

const getCertificateByToken = `-- name: GetCertificateByToken :one
SELECT id, enrollment_id, user_id, course_id, tenant_id, certificate_number, verification_token, learner_name, course_title, creator_name, issued_at, status, revoked_reason, revoked_at, created_at FROM certificates WHERE verification_token = $1 AND status = 'active'
`

func (q *Queries) GetCertificateByToken(ctx context.Context, verificationToken string) (Certificate, error) {
	row := q.db.QueryRow(ctx, getCertificateByToken, verificationToken)
	var i Certificate
	err := row.Scan(
		&i.ID,
		&i.EnrollmentID,
		&i.UserID,
		&i.CourseID,
		&i.TenantID,
		&i.CertificateNumber,
		&i.VerificationToken,
		&i.LearnerName,
		&i.CourseTitle,
		&i.CreatorName,
		&i.IssuedAt,
		&i.Status,
		&i.RevokedReason,
		&i.RevokedAt,
		&i.CreatedAt,
	)
	return i, err
}

const getNextCertificateNumber = `-- name: GetNextCertificateNumber :one
SELECT 'CERT-' || to_char(NOW(), 'YYYY') || '-' || LPAD((COUNT(*) + 1)::text, 6, '0') as cert_number FROM certificates WHERE created_at >= date_trunc('year', NOW())
`

func (q *Queries) GetNextCertificateNumber(ctx context.Context) (interface{}, error) {
	row := q.db.QueryRow(ctx, getNextCertificateNumber)
	var cert_number interface{}
	err := row.Scan(&cert_number)
	return cert_number, err
}

const listCertificatesByUser = `-- name: ListCertificatesByUser :many
SELECT id, enrollment_id, user_id, course_id, tenant_id, certificate_number, verification_token, learner_name, course_title, creator_name, issued_at, status, revoked_reason, revoked_at, created_at FROM certificates WHERE user_id = $1 AND status = 'active' ORDER BY issued_at DESC
`

func (q *Queries) ListCertificatesByUser(ctx context.Context, userID uuid.UUID) ([]Certificate, error) {
	rows, err := q.db.Query(ctx, listCertificatesByUser, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Certificate{}
	for rows.Next() {
		var i Certificate
		if err := rows.Scan(
			&i.ID,
			&i.EnrollmentID,
			&i.UserID,
			&i.CourseID,
			&i.TenantID,
			&i.CertificateNumber,
			&i.VerificationToken,
			&i.LearnerName,
			&i.CourseTitle,
			&i.CreatorName,
			&i.IssuedAt,
			&i.Status,
			&i.RevokedReason,
			&i.RevokedAt,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const revokeCertificate = `-- name: RevokeCertificate :exec
UPDATE certificates SET status = 'revoked', revoked_reason = $2, revoked_at = NOW() WHERE id = $1
`

type RevokeCertificateParams struct {
	ID            uuid.UUID `json:"id"`
	RevokedReason *string   `json:"revoked_reason"`
}

func (q *Queries) RevokeCertificate(ctx context.Context, arg RevokeCertificateParams) error {
	_, err := q.db.Exec(ctx, revokeCertificate, arg.ID, arg.RevokedReason)
	return err
}
