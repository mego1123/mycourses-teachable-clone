// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.27.0
// source: course_coupons.sql

package gen

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const createCoupon = `-- name: CreateCoupon :one
INSERT INTO course_coupons (tenant_id, code, discount_type, discount_value, currency, course_id, expires_at, usage_limit)
VALUES ($1, UPPER($2), $3, $4, $5, $6, $7, $8) RETURNING id, tenant_id, code, discount_type, discount_value, currency, course_id, expires_at, usage_limit, used_count, is_active, created_at, updated_at
`

type CreateCouponParams struct {
	TenantID      uuid.UUID   `json:"tenant_id"`
	Upper         interface{} `json:"upper"`
	DiscountType  string      `json:"discount_type"`
	DiscountValue int32       `json:"discount_value"`
	Currency      string      `json:"currency"`
	CourseID      *uuid.UUID  `json:"course_id"`
	ExpiresAt     *time.Time  `json:"expires_at"`
	UsageLimit    *int32      `json:"usage_limit"`
}

func (q *Queries) CreateCoupon(ctx context.Context, arg CreateCouponParams) (CourseCoupon, error) {
	row := q.db.QueryRow(ctx, createCoupon,
		arg.TenantID,
		arg.Upper,
		arg.DiscountType,
		arg.DiscountValue,
		arg.Currency,
		arg.CourseID,
		arg.ExpiresAt,
		arg.UsageLimit,
	)
	var i CourseCoupon
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.Code,
		&i.DiscountType,
		&i.DiscountValue,
		&i.Currency,
		&i.CourseID,
		&i.ExpiresAt,
		&i.UsageLimit,
		&i.UsedCount,
		&i.IsActive,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const deleteCoupon = `-- name: DeleteCoupon :exec
DELETE FROM course_coupons WHERE id = $1
`

func (q *Queries) DeleteCoupon(ctx context.Context, id uuid.UUID) error {
	_, err := q.db.Exec(ctx, deleteCoupon, id)
	return err
}

const getCouponByID = `-- name: GetCouponByID :one
SELECT id, tenant_id, code, discount_type, discount_value, currency, course_id, expires_at, usage_limit, used_count, is_active, created_at, updated_at FROM course_coupons WHERE id = $1
`

func (q *Queries) GetCouponByID(ctx context.Context, id uuid.UUID) (CourseCoupon, error) {
	row := q.db.QueryRow(ctx, getCouponByID, id)
	var i CourseCoupon
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.Code,
		&i.DiscountType,
		&i.DiscountValue,
		&i.Currency,
		&i.CourseID,
		&i.ExpiresAt,
		&i.UsageLimit,
		&i.UsedCount,
		&i.IsActive,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const incrementCouponUsage = `-- name: IncrementCouponUsage :exec
UPDATE course_coupons SET used_count = used_count + 1 WHERE id = $1
`

func (q *Queries) IncrementCouponUsage(ctx context.Context, id uuid.UUID) error {
	_, err := q.db.Exec(ctx, incrementCouponUsage, id)
	return err
}

const listCouponsByTenant = `-- name: ListCouponsByTenant :many
SELECT id, tenant_id, code, discount_type, discount_value, currency, course_id, expires_at, usage_limit, used_count, is_active, created_at, updated_at FROM course_coupons WHERE tenant_id = $1 ORDER BY created_at DESC LIMIT $2 OFFSET $3
`

type ListCouponsByTenantParams struct {
	TenantID uuid.UUID `json:"tenant_id"`
	Limit    int32     `json:"limit"`
	Offset   int32     `json:"offset"`
}

func (q *Queries) ListCouponsByTenant(ctx context.Context, arg ListCouponsByTenantParams) ([]CourseCoupon, error) {
	rows, err := q.db.Query(ctx, listCouponsByTenant, arg.TenantID, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []CourseCoupon{}
	for rows.Next() {
		var i CourseCoupon
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.Code,
			&i.DiscountType,
			&i.DiscountValue,
			&i.Currency,
			&i.CourseID,
			&i.ExpiresAt,
			&i.UsageLimit,
			&i.UsedCount,
			&i.IsActive,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const validateCoupon = `-- name: ValidateCoupon :one
SELECT id, tenant_id, code, discount_type, discount_value, currency, course_id, expires_at, usage_limit, used_count, is_active, created_at, updated_at FROM course_coupons WHERE tenant_id = $1 AND code = UPPER($2) AND is_active = TRUE AND (expires_at IS NULL OR expires_at > NOW()) AND (usage_limit IS NULL OR used_count < usage_limit)
`

type ValidateCouponParams struct {
	TenantID uuid.UUID   `json:"tenant_id"`
	Upper    interface{} `json:"upper"`
}

func (q *Queries) ValidateCoupon(ctx context.Context, arg ValidateCouponParams) (CourseCoupon, error) {
	row := q.db.QueryRow(ctx, validateCoupon, arg.TenantID, arg.Upper)
	var i CourseCoupon
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.Code,
		&i.DiscountType,
		&i.DiscountValue,
		&i.Currency,
		&i.CourseID,
		&i.ExpiresAt,
		&i.UsageLimit,
		&i.UsedCount,
		&i.IsActive,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
