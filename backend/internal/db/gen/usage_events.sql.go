// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.27.0
// source: usage_events.sql

package gen

import (
	"context"
	"encoding/json"
	"time"

	"github.com/google/uuid"
)

const createUsageEvent = `-- name: CreateUsageEvent :one
INSERT INTO usage_events (tenant_id, user_id, type, amount, metadata) VALUES ($1, $2, $3, $4, $5) RETURNING id, tenant_id, user_id, type, amount, metadata, created_at
`

type CreateUsageEventParams struct {
	TenantID uuid.UUID       `json:"tenant_id"`
	UserID   *uuid.UUID      `json:"user_id"`
	Type     string          `json:"type"`
	Amount   int64           `json:"amount"`
	Metadata json.RawMessage `json:"metadata"`
}

func (q *Queries) CreateUsageEvent(ctx context.Context, arg CreateUsageEventParams) (UsageEvent, error) {
	row := q.db.QueryRow(ctx, createUsageEvent,
		arg.TenantID,
		arg.UserID,
		arg.Type,
		arg.Amount,
		arg.Metadata,
	)
	var i UsageEvent
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.UserID,
		&i.Type,
		&i.Amount,
		&i.Metadata,
		&i.CreatedAt,
	)
	return i, err
}

const getUsageSummary = `-- name: GetUsageSummary :one
SELECT COALESCE(SUM(amount), 0)::bigint as total FROM usage_events WHERE tenant_id = $1 AND created_at >= $2 AND created_at < $3
`

type GetUsageSummaryParams struct {
	TenantID    uuid.UUID `json:"tenant_id"`
	CreatedAt   time.Time `json:"created_at"`
	CreatedAt_2 time.Time `json:"created_at_2"`
}

func (q *Queries) GetUsageSummary(ctx context.Context, arg GetUsageSummaryParams) (int64, error) {
	row := q.db.QueryRow(ctx, getUsageSummary, arg.TenantID, arg.CreatedAt, arg.CreatedAt_2)
	var total int64
	err := row.Scan(&total)
	return total, err
}
